vbell off
autodetach on
startup_message off
defscrollback 32000

# Always start `screen` with UTF-8 enabled (`screen -U`)
defutf8 on
term xterm-color
termcapinfo xterm ti@:te@ # scroll wheel action!
escape ^Tt # Instead of Control-a, make the escape/command character be Control-b
shell -$SHELL
